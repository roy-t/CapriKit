{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "$j = [Console]::In.ReadToEnd() | ConvertFrom-Json; $f = $j.tool_input.file_path; if ($f -and $f.EndsWith('.cs')) { $rel = [IO.Path]::GetRelativePath($PWD.Path, $f); dotnet format CapriKit.slnx --include $rel --verbosity quiet }",
            "timeout": 120,
            "statusMessage": "Running dotnet format..."
          }
        ]
      }
    ]
  }
}
